# 第十五章
#### 1. 创建实例作用：

以代码里的骰子为例：

```python
class Die:
    def __init__(self, num_sides=6):  # 模板：定义骰子长什么样
        self.num_sides = num_sides

die = Die()  # 实例：造出一个具体的骰子
```

**创建实例的意义：**
1.同一个类可以创建多个不同的实例：
```python
die_6 = Die(6)   # 6面骰子
die_10 = Die(10) # 10面骰子
die_20 = Die(20) # 20面骰子
```

三个实例互不干扰，各自有自己的 `num_sides`。

2.通过实例调用类的功能：

```python
die.roll()       # 投骰子
```

==**一句话总结==：** 类定义了"能做什么"，实例是"真正去做的那个对象"，没有实例，类里的方法和数据都没法用。

#### 2.  fig.update_layout(xaxis_dtick=1)

`fig.update_layout(xaxis_dtick=1)` 的作用是**设置 x 轴刻度的间隔为 1**。

**语法拆解：**
- `fig.update_layout()` — plotly 中用来修改图表整体布局的方法
- `xaxis_dtick` — `xaxis`（x轴）+ `dtick`（delta tick，刻度间距）
- `= 1` — 每隔1个单位显示一个刻度

==**为什么要用它：**==
默认情况下 plotly 会自动决定刻度间距，比如骰子有6个面，x轴可能显示 `1, 3, 5` 或者 `2, 4, 6`，不够直观。
加了这行后 x 轴会显示 `1, 2, 3, 4, 5, 6`，每个骰子点数都有对应的刻度，图表更清晰。

#### 3.for循环和列表推导式
列表推导式是一种用一行代码生成列表的简洁写法。
**普通写法 vs 列表推导式：**
```python
# 普通 for 循环
squares = []
for x in range(10):
	square = x**2
    squares.append(square)

# 列表推导式——同样的结果，一行搞定
squares = [x**2 for x in range(10)]
```

**语法结构：**

```
[表达式 for 变量 in 可迭代对象]
```

**再看一个例子，只要偶数：**

```python
# 普通写法
evens = []
for x in range(10):
    if x % 2 == 0:
        evens.append(x)

# 列表推导式
evens = [x for x in range(10) if x % 2 == 0]
```

**带条件的语法结构：**

```
[表达式 for 变量 in 可迭代对象 if 条件]
```

---

对应到你的骰子代码，比如掷1000次取结果：

```python
# 普通写法
results = []
for roll_num in range(1000):
    result = die.roll()
    results.append(result)

# 列表推导式
results = [die.roll() for _ in range(1000)]
```

注意这里用 `_` 代替 `roll_num`，因为循环变量根本没被用到，`_` 表示"这个变量我不在乎"。

#### 4.各种表格的使用（==后续需要添加的==）
1. bar的使用
`px.bar()` 是 Plotly Express 的柱状图函数，参数解释：
```python
fig = px.bar(
    x = range(2, 12),             # 横轴的值：每根柱子的位置（点数 2~12）
    y = frequencies,              # 纵轴的值：每根柱子的高度（出现次数）
    title = title,                # 图表标题
    labels = labels               # 轴标签，{'x': '横轴名', 'y': '纵轴名'}
)
```

**数据对应关系：**

|x（点数）|y（频率）|
|---|---|
|2|frequencies[0]|
|3|frequencies[1]|
|...|...|
|12|frequencies[10]|

x 和 y 必须长度一样，一一对应，每对数据画一根柱子。

**注意：** 你现在写的是 `range(1, max_result+1)`，起点是1，但两个D6骰子最小点数是2，应该改成 `range(2, max_result+1)`。




# 第十六章
## 16.1折线图
### 16.1.1解析csv文件头
```python
from pathlib import Path
import csv
path = Path('D:\代码\PY_test\第十六章\csv\sitka_weather_07-2021_simple.csv')
lines = path.read_text().splitlines()

reader = csv.reader(lines)
header_row = next(reader)
print(header_row)

#16.1.2打印文件头及其位置
for index, column_header in enumerate(header_row):
    print(index, column_header)
    
    
#16.1.3提取并读取数据
#highs = []
#for row in reader:
#    high = int(row[4])
#    highs.append(high)
#列表推导式表达
highs = [int(row[4]) for row in reader ]
print(highs)
```

1. cvs是python内置模块。
2. lines = path.read_text().splitlines()
这行是三个方法链式调用：
```python
lines = path.read_text().splitlines()
```

**分解来看：**
**1. `path.read_text()`**
- 把整个文件读成一个大字符串
- 结果：读取文档中所有内容

**2. `.splitlines()`**
- 把字符串按换行符 `\n` 切割，返回一个列表，按行分割
- 结果：将完整的文档分成行

**3. `lines =`**
- 把这个列表赋值给变量 `lines`


3. header_row = next(reader)
next() 是 Python 内置函数，作用是从迭代器中取出下一个元素。
为什么 next(reader) 就是第一行：

`reader` 是迭代器，内部有一个"指针"记录当前读到哪里，初始指向第一行。
```
调用前：指针 → ["date", "temp"]  ← 第1行
              ["2024-01-01","25"] ← 第2行
              ["2024-01-02","28"] ← 第3行

调用 next(reader) 后：
    返回值：["date", "temp"]      ← 取出第1行
    指针 →  ["2024-01-01","25"]  ← 指针移到第2行
```
之后再用 `for row in reader`，就从第2行开始遍历，不会重复读表头，这是一个固定搭配：
```python
header_row = next(reader)  # 取出第1行（表头）
for row in reader:         # 从第2行开始遍历数据
    print(row)
```


4. index索引、column_header列表头
**`enumerate()` 的作用：**
普通 for 循环只能拿到值，enumerate() 能同时拿到序号和值：
```python
# 普通循环
for column_header in header_row:
    print(column_header)  # 只有列名

# enumerate() 循环
for index, column_header in enumerate(header_row):
    print(index, column_header)  # 同时有序号和列名
```

输出结果类似：
```
0  STATION
1  NAME
2  DATE
3  TAVG
4  TMAX
5  TMIN
```

**这样做的目的：** 找到你需要的数据在第几列（比如最高温在第4列），后面取数据时就用 `row[4]` 来提取，非常实用。

5. int() 的作用：把字符串转成整数。

CSV 文件里所有数据读出来都是**字符串**，即使看起来是数字：
```python
row[4]        # "25"  ← 字符串，不能做数学运算
int(row[4])   # 25   ← 整数，可以做数学运算
```

**为什么必须转换：**
```python
"25" + "10"   # "2510"  ← 字符串拼接，不是加法
25 + 10       # 35      ← 正确的数学运算
```

后面画图、求最大值、求平均值都需要数字，所以必须先用 `int()` 转换。

**常用的类型转换函数：**

|    函数     |    作用    |            例子            |
| :-------: | :------: | :----------------------: |
|  `int()`  |   转整数    |    `int("25")` → `25`    |
| `float()` | 转小数(浮点数) | `float("25.5")` → `25.5` |
|  `str()`  |   转字符串   |    `str(25)` → `"25"`    |

温度数据用 `int()` 就够了，如果是带小数的数据（比如降水量）就要用 `float()`。


### 16.1.4绘制温度图
```python
from pathlib import Path
import csv
import matplotlib.pyplot as plt

path = Path('D:\代码\PY_test\第十六章\csv\sitka_weather_07-2021_simple.csv')
lines = path.read_text().splitlines()
reader = csv.reader(lines)
head_row = next(reader)

highs = [int(row[4]) for row in reader]

plt.style.use('seaborn-v0_8')
fig, ax = plt.subplots()
ax.plot(highs, color = 'red')
ax.set_title('Daily High Temperatures, July 2021', fontsize = 24)
ax.set_xlabel('', fontsize = 16)
ax.set_ylabel('Temperature (F)', fontsize = 16)
ax.tick_params(labelsize = 16)
plt.show()
```

```python
fig, ax = plt.subplots()
```
创建一个图表窗口，返回两个对象：

|  变量   |    含义    |      用途       |
| :---: | :------: | :-----------: |
| `fig` |  整个图表窗口  |  设置图片大小、保存图片  |
| `ax`  | 图表里的坐标区域 | 画线、画点、设置标题轴标签 |

类比：`fig` 是画框，`ax` 是画框里的画布，实际画图都在 `ax` 上操作：

```python
ax.plot(x, y)        # 画折线
ax.set_title('标题') # 设置标题
ax.set_xlabel('横轴') # 设置横轴标签
```


### 16.1.5datetime模块[[datetime模块中设置日期和时间格式的参数]]
[datetime 模块中设置日期和时间格式的参数.md](https://github.com/niuyiqun886/codedex-python/blob/main/datetime模块中设置日期和时间格式的参数.md)
```python
from datetime import datetime
first_date = datetime.strptime('2021-07-01', '%Y-%m-%d')
print(first_date)
```
1. 从datetime导入datetime类
2. `strptime()` 把字符串转成日期对象（str parse time）。
两个参数：
- 第一个：要转换的日期字符串 `'2021-07-01'`
- 第二个：告诉函数字符串的格式 `'%Y-%m-%d'`

**格式符含义：**

| 格式符  | 含义   | 例子   |
| ---- | ---- | ---- |
| `%Y` | 四位年份 | 2021 |
| `%m` | 两位月份 | 07   |
| `%d` | 两位日期 | 01   |

为什么要转换：
CSV 里的日期是字符串 `'2021-07-01'`，字符串没法排序、没法在图表横轴上正确显示为时间刻度。转成 `datetime` 对象后，matplotlib 能自动识别并正确渲染日期轴。

```python
'2021-07-01'          # 字符串，matplotlib不认识
datetime(2021, 7, 1)  # 日期对象，matplotlib能正确显示
```

注：如果是序号的话需要int()一下
```python
x = [int(row[0]) for row in reader]  # 字符串→整数，搞定
```

### 16.1.6在图中添加日期
```python
from pathlib import Path
import csv
from datetime import datetime
import matplotlib.pyplot as plt

path = Path('D:\代码\PY_test\第十六章\csv\sitka_weather_07-2021_simple.csv')
lines = path.read_text().splitlines()

reader = csv.reader(lines)
header_row = next(reader)

#dates, highs = [], []
#for row in reader:
#    current_date = datetime.strptime(row[2], '%Y-%m-%d')
#    dates.append(current_date)
#    high = int(row[4])
#    highs.append(high)

rows = list(reader)                  #使用列表将每一行存下来，然后遍历这个列表
dates = [datetime.strptime(row[2], '%Y-%m-%d') for row in rows]
highs = [int(row[4]) for row in rows]

plt.style.use('seaborn-v0_8')
fig, ax = plt.subplots()
ax.plot(dates, highs, color = 'red')

#设置绘图格式
ax.set_title('Daily High Temperatures, July 2021', fontsize = 24)
ax.set_xlabel('', fontsize = 16)
fig.autofmt_xdate()

ax.set_ylabel('Tmeperature (F)', fontsize = 16)
ax.tick_params(labelsize = 16)
path_fig = 'D:\代码\PY_test\第十六章\png/16_1_6.png'
plt.savefig(path_fig, bbox_inches = 'tight', dpi = 500)
plt.show()

print(rows)
```


1. 主要是现将字符串形式的日期和行号转化成日期形式和数字形式。
使用datetime.strptime()和int()两个函数

2. ax.plot(dates, highs, color='red')
`ax.plot()` 是画**折线图**的函数。

**三个参数：**

|参数|含义|这里的值|
|---|---|---|
|第一个位置参数|横轴数据|`dates`（日期列表）|
|第二个位置参数|纵轴数据|`highs`（温度列表）|
|`color`|线的颜色|`'red'` 红色|

**常用可选参数：**

```python
ax.plot(dates, highs,
    color='red',        # 颜色
    linewidth=2,        # 线宽
    linestyle='--',     # 线型：实线'-' 虚线'--' 点线':'
    alpha=0.5,          # 透明度 0~1
    label='最高温度'    # 图例标签
)
```

3. fig.autofmt_xdate()
自动旋转横轴的日期标签，防止日期文字互相重叠。

**不调用时：**
```
2021-07-01 2021-07-02 2021-07-03 ...
← 日期文字挤在一起，看不清
```

**调用后：**
```
2021-07-01
  2021-07-02
    2021-07-03
← 日期斜着显示，不重叠
```

默认会把日期标签旋转约30度并右对齐，只针对日期类型的横轴有效，普通数字轴不需要用这个。

4. 这里使用列表推导式的时候出现的问题：
问题：就是这里的reader是一个一个的列表，即每行是一个单独的列表。所以直接用列表推导式遍历的时候就会只遍历第一个，无法全遍历。
解决：将reader里的所有列表放到一个列表中，这样的话再遍历新的列表就会遍历到每一个reader中的列表了。
reader：[]，[]，[]，........；list(reader)后的[[]，[]，[]，.......]

### 16.9.1给图形区域着色
```python
plt.style.use('seaborn-v0_8')
fig, ax = plt.subplots()
ax.plot(dates, highs, color = 'red', alpha = 0.5)
ax.plot(dates, lows, color = 'blue', alpha = 0.5)
ax.fill_between(dates, highs, lows, facecolor = 'blue', alpha = 0.1)
```

1. alpha参数：表示的是通明度，取值范围0~1
2. fill_between()：两条曲线之间填充颜色的函数。
```python
#基本语法格式
ax.fill_between(x, y1, y2)
ax.plot(dates, highs)   # 上线
ax.plot(dates, mids)    # 中线
ax.plot(dates, lows)    # 下线

# 填充上线和中线之间
ax.fill_between(dates, highs, mids, facecolor='red', alpha=0.1)

# 填充中线和下线之间
ax.fill_between(dates, mids, lows, facecolor='blue', alpha=0.1)

```


16.1.10错误检查
1. csv文件中出现了空的表格(即出现了字符串的缺失，空字符串)
```python
dates, highs, lows = [], [], []
#for row in reader:
#    date = datetime.strptime(row[2], '%Y-%m-%d')
#    dates.append(date)
#    high = int(row[3])
#    highs.append(high)
#    low = int(row[4])
#    lows.append(low)

for row in reader:
    date = datetime.strptime(row[2], '%Y-%m-%d')
    try:
        high = int(row[3])                  # 尝试转换数据
        low = int(row[4])
    except ValueError:                      # 如果转换失败（数据缺失/空值）
        print(f"Missing data for {date}")   # 打印提示，跳过这行
    else:                                   # 如果try成功，没有报错
        dates.append(date)                  # 才把数据加入列表
        highs.append(high)
        lows.append(low)
```

解决办法：使用try ---except ValueError-----else语句，异常处理结构，用于处理数据缺失的情况。

**为什么需要这个：**
CSV 气象数据里某些天可能有缺失值，比如：
```
2021-07-15,,          ← 温度是空的
```

`int('')` 会抛出 `ValueError`，直接导致程序崩溃。用 `try/except` 捕获这个错误，程序就能跳过坏数据继续运行。

**关键细节：** 数据放在 `else` 里追加而不是 `try` 里，是为了确保只有完整的一行数据（高温和低温都成功转换）才会被保存，避免出现 `dates` 和 `highs` 长度不一致的问题。

注：缺失数据的处理

缺失的数据**根本不会被加入列表**，所以画图时那天直接被跳过了。
```python
except ValueError:
    print(f"Missing data for {date}")  # 只打印，不append
else:
    dates.append(date)   # 只有成功才加入
    highs.append(high)
    lows.append(low)
```

比如7月15日数据缺失：

```
dates:  [7-13, 7-14,       7-16, 7-17]  ← 没有7-15
highs:  [64,   68,         70,   72  ]
```

matplotlib 画折线时会直接把 7-14 和 7-16 **连起来**，中间不会有断点，看起来就像那天不存在一样。

---

**如果你想让缺失处显示为断开的线**，才需要用 `None` 占位：

```python
except ValueError:
    dates.append(date)
    highs.append(None)   # None会让折线在此处断开
    lows.append(None)
```

但书里的处理方式是直接跳过，更简单。


练习16.4：自动索引==(这个以后整理的时候会用到，只需要改变地址就行了)==
```python
from pathlib import Path
import csv
from datetime import datetime
import  matplotlib.pyplot as plt

path = Path('D:\代码\PY_test\第十六章\csv\death_valley_2021_simple.csv')
lines = path.read_text().splitlines()

reader = csv.reader(lines)
header_row = next(reader)

#for index, column in enumerate(header_row):   #采用for循环遍历的形式
#    if column == 'TMAX':
#        column_TMAX = index
#        print(column_TMAX)
#    if column == 'TMIN':
#        column_TMIN = index
#        print(column_TMIN)
#   if column == 'DATE':
#        column_date = index
#        print(column_date)
#    if column == 'NAME':
#        column_name = index
#        print(column_name)

##简化
column_TMAX = header_row.index('TMAX')
column_TMIN = header_row.index('TMIN')
column_date = header_row.index('DATE')
column_name = header_row.index('NAME')

rows = list(reader)
station_name = rows[0][column_name]    #先取rows的第一行，然后再取 `[column]` 这个位置的内容。
print(station_name)

dates, highs, lows = [], [], []
for row in rows:                 #上面将reader变为了rows了，所以要遍历rows
    date = datetime.strptime(row[column_date], '%Y-%m-%d')
    try:
        high = int(row[column_TMAX])
        low = int(row[column_TMIN])
    except ValueError:
        #dates.append(date)
        #highs.append(None)   # None会让折线在此处断开
        #lows.append(None)
        print(f"Missing data for {date}")
    else:
        dates.append(date)
        highs.append(high)
        lows.append(low)

plt.style.use('seaborn-v0_8')
fig, ax = plt.subplots()
ax.plot(dates, highs, color = 'red', alpha = 0.5)
ax.plot(dates, lows, color = 'blue', alpha = 0.5)
ax.fill_between(dates, highs, lows, facecolor = 'blue', alpha = 0.1)

ax.set_title(f"{station_name}", fontsize = 24)
ax.set_xlabel('', fontsize = 16)
fig.autofmt_xdate()

ax.set_ylabel('Temperature (F)', fontsize = 16)
ax.tick_params(labelsize = 16)
path_fig = 'D:\代码\PY_test\第十六章\png/ex_16_4.png'
plt.savefig(path_fig, bbox_inches = 'tight', dpi = 500)
plt.show()
```

目的：是将里面的所有的硬编码的方式变成索引的形式。

1. `list.index('值')` 在列表里查找某个值，返回它的位置序号。 （这个和for循环找是一样的）
举例：
```python
header_row = ['STATION', 'NAME', 'DATE', 'TMAX', 'TMIN']

header_row.index('TMAX')  # 返回 3
header_row.index('DATE')  # 返回 2
```
2.  `station_name = rows[0][column_name]`
这是两层列表索引，拆开来看：就是先取rows的第一行，然后再取 `[column]` 这个位置的内容。

**`rows` 的结构：**
```python
rows = [
    ['DEATH VALLEY', '2021-01-01', '68', '40'],  # rows[0] 第1行
    ['DEATH VALLEY', '2021-01-02', '72', '45'],  # rows[1] 第2行
    ...
]
```

**`rows[0]`** — 取第一行（一个列表）：
```python
['DEATH VALLEY', '2021-01-01', '68', '40']
```

**`rows[0][column_name]`** — 再取这一行里 `column_name` 位置的值：
```python
'DEATH VALLEY'
```



## 16.2散点图
### 16.2.1主要是.geojson文件(里面包含一些字典)
### 16.2.2查看GeoJSON数据
```python
from pathlib import Path
import json

#将数据作为字符串读取并转换为 Python 对象
path = Path(r'D:\代码\PY_test\第十六章\geojson\eq_data_1_day_m1.geojson')  #前面采用r代表原始字符串
connects = path.read_text()
all_eq_data = json.loads(connects)     #将字符串(eq_data_1_day_m1.geojson)转换为connects转换成字典的形式，方便取value
#print(all_eq_data)

#将数据文件转换为更易读的版本
path = Path(r'D:\代码\PY_test\第十六章\geojson\readable_eq_data.geojson')
readable_connects = json.dumps(all_eq_data, indent = 4)   #将字典形式的all_eq_data转换为字符串的形式存起来      indent是指定json出输出时的缩进个数，让文件格更易读
path.write_text(readable_connects)

print(type(all_eq_data))                 #可以用type()这个函数看这两个文档属于什么类型，字符串还是字典
print(type(readable_connects))


###16.2.5提取位置数据
mags, titles, lons, lats = [], [], [], []
for eq_dict in all_eq_dicts:
    mag = eq_dict['properties']['mag']
    title = eq_dict['properties']['title']
    lon = eq_dict['geometry']['coordinates'][0]   #多层嵌套取值
    lat = eq_dict['geometry']['coordinates'][1]
    mags.append(mag)
    titles.append(title)
    lons.append(lon)
    lats.append(lat)
     
print(mags[:10])            #截取从第1个到第10个
print(titles[:2])
print(lons[:5])
print(lats[:5])


###16.2.7指定数据的另一种形式，封装数据
import plotly.express as px
import pandas as pd
data = pd.DataFrame(
    data = zip(lons, lats, titles, mags), columns = ['经度', '纬度', '位置',
    '震级']
)

data.head()
```
注：
1. 路径中前面加的 `r` ：读取路径的时候报错了，
`r` 表示 **raw string（原始字符串）**，让 Python 不把反斜杠 `\` 当成转义字符。

Python 会把这些组合当成特殊字符：

|组合|Python 理解成|
|---|---|
|`\n`|换行符|
|`\t`|制表符|
|`\g`|非法转义（报错或警告）|
|`\e`|非法转义|

你这个路径里的 `\geojson` 开头是 `\g`，`\eq_data` 开头是 `\e`，都会出问题。

2. json.load()：  将字符串的文件形式转化为字典的形式，然后字典就可以使用[]取值了。

`json.loads()` 把 **JSON 格式的字符串**转换成 **Python 对象**（通常是字典）。

转换前后对比：
```python
connects       # 字符串，一大坨文本，不能用 [] 取值
'{"type": "FeatureCollection", "features": [...]}'

all_eq_data    # 字典，可以用 [] 取值
{'type': 'FeatureCollection', 'features': [...]}
```

转换后就能这样访问数据了：
```python
all_eq_data['features']
all_eq_data['metadata']['title']
```


函数名的含义：

|函数|全称|方向|
|---|---|---|
|`json.loads()`|load **s**tring|字符串 → Python对象|
|`json.dumps()`|dump **s**tring|Python对象 → 字符串|

末尾的 `s` 指的是 string，两个函数互为反操作。你代码第12行的 `json.dumps()` 就是把字典转回字符串好写入文件。

JSON 与 Python 类型对应关系：

|JSON|Python|
|---|---|
|object `{}`|dict|
|array `[]`|list|
|string|str|
|number|int / float|
|true / false|True / False|
|null|None|

3.  .write_text()：[[python文件保存形式]]

`write_text()` 把字符串写入文件，如果文件不存在会自动创建，如果已存在会覆盖原有内容。

**Path 对象的读写方法：**

|方法|作用|数据类型|
|---|---|---|
|`path.read_text()`|读取文本文件|返回 str|
|`path.write_text(内容)`|写入文本文件|传入 str|
|`path.read_bytes()`|读取二进制文件|返回 bytes|
|`path.write_bytes(内容)`|写入二进制文件|传入 bytes|

`read_bytes` / `write_bytes` 用于图片、视频、压缩包这类非文本文件。

**常用参数：**

```python
path.write_text(内容, encoding='utf-8')   # 指定编码，写中文时建议加上
path.read_text(encoding='utf-8')
```

Windows 默认编码不是 utf-8，处理中文内容时不加这个参数容易出乱码或报错。

---

**其他文件类型的处理方式：**

|文件类型|用什么|
|---|---|
|`.txt`|`read_text()` / `write_text()`|
|`.json`|`json.loads()` + `read_text()`|
|`.csv`|`csv.reader()` + `read_text().splitlines()`|
|`.xlsx`|`pandas.read_excel()` / `df.to_excel()`|
|图片、二进制|`read_bytes()` / `write_bytes()`|

注意 JSON 和 CSV 本质上也是文本文件，只是需要额外的模块来**解析**内容结构，读写文件这一步还是用 `read_text()`。

4. indent：
`indent=4` 指定 JSON 输出时的**缩进空格数**，让文件格式化成易读的样子。

**不加 `indent`（默认）： **全挤在一行，没法看。
```json
{"type": "FeatureCollection", "metadata": {"title": "M1.0+ Earthquakes"}, "features": [...]}
```

**加了 `indent=4`： **每层嵌套缩进 4 个空格，层次清晰。
```json
{
    "type": "FeatureCollection",
    "metadata": {
        "title": "M1.0+ Earthquakes"
    },
    "features": [...]
}
```

**常用值：**

|值|效果|
|---|---|
|`indent=None`（默认）|压缩成一行，文件最小|
|`indent=2`|缩进2空格，紧凑一些|
|`indent=4`|缩进4空格，最常用|

代价是文件体积变大。给人看用 `indent=4`，程序之间传输数据用默认的压缩格式。

5. 多层嵌套取值

这两行都是多层嵌套取值，一层层往里剥。

**第一行：** 取到的值是key：properties中的key：title中的value：M 1.6 - 8km NE of Aguanga, CA的值
```python
title = eq_dict['properties']['title']
```

数据结构：
```python
eq_dict = {
    'type': 'Feature',
    'properties': {           # ← 第一层：字典
        'mag': 1.6,
        'title': 'M 1.6 - 8km NE of Aguanga, CA'   # ← 第二层：取这个
    },
    'geometry': {...}
}
```

**第二行：** 取的是key：geometry中的key：coordinates中的第一个value：
```python
lon = eq_dict['geometry']['coordinates'][0]
```

数据结构：
```python
eq_dict = {
    'geometry': {                              # ← 第一层：字典
        'coordinates': [-116.7, 33.4, 3.2]     # ← 第二层：列表
    }                      ↑      ↑     ↑
}                      [0]经度 [1]纬度 [2]深度
```

6. 使用pandas工具，封装数据，就是给数据创建一个表格
```python
data = pd.DataFrame(
    data = zip(lons, lats, titles, mags),
    columns = ['经度', '纬度', '位置', '震级']
)
data.head()
```

**`zip()` 的作用：把多个列表"按位置配对"** 原来是"竖着的4个列表"，`zip` 后变成"横着的3行记录"。
```python
lons  = [-116.7, -150.2, 120.5]
lats  = [33.4,   61.3,   23.8]
titles= ['A',    'B',    'C']
mags  = [1.6,    2.3,    4.1]

zip(lons, lats, titles, mags)
# 变成一行一行的组合：
# (-116.7, 33.4, 'A', 1.6)
# (-150.2, 61.3, 'B', 2.3)
# (120.5,  23.8, 'C', 4.1)
```

**`pd.DataFrame()` 的作用：造一张表格**

|经度|纬度|位置|震级|
|---|---|---|---|
|-116.7|33.4|A|1.6|
|-150.2|61.3|B|2.3|
|120.5|23.8|C|4.1|

- `data=` 传入数据内容
- `columns=` 给每一列起名字

**`data.head()`：查看表格前5行**

用来快速确认表格造得对不对。不过在 `.py` 文件里单独写 `data.head()` 是**看不到输出的**，要加 `print()`：
```python
print(data.head())
```

**为什么要转成 DataFrame：**
因为后面 `px.scatter()` 可以直接用列名来指定数据：
```python
px.scatter(data, x='经度', y='纬度', color='震级')
```

比传4个独立列表清晰得多，鼠标悬停也能自动显示对应信息。